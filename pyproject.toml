[tool.pytest.ini_options]
testpaths = ["tests"]
# archiveディレクトリを無視してエラーを回避
norecursedirs = ["archive", ".venv-ocr27", "scripts/archive"]
python_files = ["test_*.py", "verify_*.py"]
python_functions = ["test_*"]
addopts = "--cov=. --cov-report=term-missing"
